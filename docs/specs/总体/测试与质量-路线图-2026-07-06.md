# 试飞控制系统 — 测试策略 & 项目进展约定 & 远期任务路线图

> 补 spec(回应审查报告 G1/G2/G4,2026-07-06)。填补原始设计稿要求的"status.md 管理项目进展""自动测试审查"产出物。
>
> 本文件分三部分:§1 测试策略(G1)、§2 `status.md` 项目进展约定(G1)、§3 远期 spec 路线图(G4 PX4 阶段2 详细 spec;G2 开放式接口规范 2026-07-07 已交付,见 §3.1)。
>
> **2026-07-07 同步**:求解器废弃 → A/B 单测从 solver mock/collision 改 small_model stub/ActionCommand schema 单测;端到端链路改 α→ActionCommand→B 侧端侧小模型/ego-planner;集成下发从 `trajectory` 改 `action`;验收门槛从求解器碰撞校验改 small_model 未知动作编码/夹紧后越界 reject。详见 [2026-07-07-架构重构-执行总报告.md](../../过时存档/过程文件/2026-07-07-架构重构-执行总报告.md)。

---

## 1. 测试策略(G1)

### 1.1 测试分层

| 层级 | 范围 | 工具 | 何时跑 |
|---|---|---|---|
| 单元 | A 侧 `bus/`(router/registry/protocol)、`analytics/`(fft/stats/filter 纯函数)、`ActionCommand` schema 校验(总体架构 §2.2 字段/`schema_version=2`/动作编码合法性)、`ipc/frames`(msgpack 长度前缀编解码) | `pytest`(venv-A) | 提交前 + CI |
| 单元 | B 侧 `small_model/goal_gen`(stub 规则映射:9 类动作编码→目标点)、`small_model/action_codes`(编码映射)、`small_model/stub`(boundary/ceiling/floor/speed_max 夹紧 + 未知动作→reject)、`monitor/thresholds`/`trends` | `pytest`(venv-B,不带 rospy 的纯函数部分) | 提交前 |
| 集成 | A↔B IPC:`S0` msgpack `use_bin_type` 互通;`S3` ping/pong + 下发 `action`(ActionCommand)+ 上行 `pose`(后端 B spec §11) | 脚本化联调 | 联调阶段 |
| 集成 | 前端↔A:`/api/chat/beta` SSE 事件(text/tool_call_start/tool_call_result/plan)、WS pose/alert/alpha_output、REST /api/proposals/* | 手动 + (可选)Playwright | 联调阶段 |
| 端到端 | β Chat → α 翻译 ActionCommand → B 侧端侧小模型 slaves→目标点→(阶段2/4 ego-planner 连续轨迹)→假无人机 → pose 回显 + alert(S5/S6,后端 B spec §11) | `rosbag` 录制 + 断言脚本 | 联调阶段 |
| 安全/断连 | `S4` reject 回路、`S7` 杀 A/B 后无人机安全悬停 | 脚本 | 联调阶段 |

### 1.2 验收门槛(先导)

- A 单元 + B 纯函数单元测试全绿。
- `S0`~`S7`(后端 B spec §11)逐阶段通过才进下一阶段。
- B 侧 small_model stub 安全舍入单测:`ActionCommand.safety_constraints` 的 `boundary`/`ceiling`/`floor`/`speed_max` 夹紧充分(夹紧后目标点必在 [floor,ceiling] 高度内且不越 boundary);未知动作编码 → 产 reject 单测覆盖。
- `ActionCommand` schema 单测:动作编码全部 9 类合法、`schema_version=2`、缺字段/类型错被 schema 校验拦截。
- msgpack 跨 venv-A(pip 最新)/venv-B(0.6.2)`use_bin_type=True` 帧互解(S0 验证项)。
- 前端不强制自动化;先导以手动验收 + 浏览器 DevTools 为主(开发计划各 P 阶段验收清单),Playwright 后补。

### 1.3 CI(可选,先导不强制定)

- 仓库可选 `.github/workflows/test.yml`:在 Ubuntu 20.04 容器内跑 A 单元(py3.10)+ B 纯函数(py3.8,不依赖 ROS),不计 ROS/gazebo。
- 先导阶段允许手动跑;阶段三联调后引入 CI。
- 测试审查 skill(原始设计稿"自动测试审查"):用 todo/test-driven-development 型流程,不自动拒绝未覆盖 PR,先导仅提示。

### 1.4 测试目录约定

```
backend-A/tests/        # pytest,纯 Python(不连 ROS/讯飞/DeepSeek,mock 外部)
backend-B/tests/        # pytest,纯函数子集(goal_gen/action_codes/stub/monitor thresholds/frames);rospy 相关用 rostest 在联调跑
```

---

## 2. `status.md` 项目进展约定(G1)

### 2.1 位置与格式

- 文件路径:`docs/status.md`(过时文档已归档至 `docs/过时存档/`；status.md 作为活动进度文件放 docs 根)。
- 格式:Markdown,固定小节,**每周**或里程碑切换时更新。
- 谁更新:责任子系统作者;Claude Code 会话切换前/后同步。

### 2.2 固定小节

```
# 项目进展 — 试飞控制系统

## 当前阶段
(阶段二核心组件开发 / 阶段三联调 / 阶段四外场)

## 各子系统状态
| 子系统 | 进度 | 阻塞 | 负责人 | 最近 commit |
|---|---|---|---|---|
| 总体架构 spec | ✅ 定稿 | — | | |
| 后端 A | 🚧 进行中(P?) | | | |
| 后端 B | | | | |
| 前端 | | | | |
| 小模型 α | ⏳ 远期(M2 未启动) | | | |

## 阻塞 / 风险
- ...

## 下一步(Next 3)
1. 2. 3.

## 联调阶段打卡(S0~S8)
- [x] S0 环境/msgpack 互通
- [ ] S1 假无人机单跑
...
```

### 2.3 与 spec 的关系

- spec(`docs/specs/`)是"设计权威",`status.md` 是"进度镜像",不重复设计内容,只记进度/阻塞/下一步。
- spec 变更走审查报告 + 用户决策(见 `docs/过时存档/过程文件/文档审查报告-*` + `文档审查报告-*-回复`);`status.md` 只记"已改/待改"标签。

---

## 3. 远期 spec 路线图(G4 余项;登记为未来任务)

> **G2 已交付**:开放式接口规范已于 **2026-07-07 架构重构本轮交付**为独立 spec [开放式接口规范.md](开放式接口规范.md)(9 节,含统一消息格式、组件注册表(9类)、6 个接口契约 ABC(`ActionTranslator`/`GoalGenerator`/`Planner`/`AnalyticsTool`/`Detector`/`DashboardDriver`)、新组件接入步骤 3 例、动态发现远期路径、验收清单 6 项全✓)。下文§3.1 仅保留作历史说明,G4 PX4 阶段 2 详细 spec 仍为远期任务。

### 3.1 开放式接口规范(G2,✅ 2026-07-07 已交付)

- **来源**:研究内容 5 / 预期产出 4"开放式接口规范文档"。
- **交付状态**:**✅ 完成**。甲商要求"完成开放式接口规范"在本轮(2026-07-07 架构重构)一并交付,提前于原设计划的"阶段三联调稳定后"。
- **交付件**:`docs/specs/总体/开放式接口规范.md`(独立 spec,9 节):
  - 统一消息格式(`{schema_version=2, from, to, msg_type, call_id, tool, args, payload, ts}`)与 `msg_type`/`tool` 语义。
  - 组件注册表(9 类:`α`/`β`/`alpha`/`small_model`/`ego_planner`(阶段2/4)/`lidar`(阶段2/4)/`monitor`/`analytics`/`dashboard_driver`;含 `to` 路由、接受 `tool`、宿主进程与生产消费语义)。
  - 6 个接口契约 ABC(`ActionTranslator`/`GoalGenerator`/`Planner`/`AnalyticsTool`/`Detector`/`DashboardDriver`)。
  - 新组件接入步骤(3 例:加入 A 侧 analytics 工具/加入 B 侧检测器/换新小模型)。
  - 动态发现/热加载演进路径(先导不做,留接口)。
  - 验收清单 6 项全✓。
- **与既有 spec 关系**:总總架构 §3.7 为"摘要",本文件为"独立固化",两者镜像;A/B/接口冻结指向本 spec 作权威接入文档。变更走 `schema_version` 升级 + 同步两侧(同 A-B 接口冻结 §9)。

### 3.2 PX4 阶段 2 详细 spec(G4) — **✅ 已交付 2026-08-02**

- **交付**:[PX4-阶段2-design.md](../后端B/PX4-阶段2-design.md) 已创建（10 节：目标/环境/编排/消息契约与 NED↔ENU 变换/Phase2Adapter offboard 状态机/改动面/安全设计/S8 验收清单/风险表/核实回填表）。
- **版本锁定修正（2026-08-02）**:`gz_x500`/Ignition 在 Ubuntu 20.04 **不可用**（二进制不发 focal，PX4 v1.14+ 弃 ROS1）；唯一可行组合 **PX4 v1.13.3 + Gazebo Classic 11 + MAVROS 1.20.1**，SITL 命令 `make px4_sitl gazebo-classic_iris`（HEADLESS=1）。
- **关键设计决策**:① BState/A/前端保持 ENU，NED↔ENU 变换单点收敛在 rosbridge（sousedb/Phase2Adapter + subscriber 注入）；② offboard 状态机 DISARMED→STREAMING(≥3s/20Hz)→ARM→OFFBOARD→ACTIVE，abort/land 一律 AUTO.LAND 兜底；③ PositionTarget type_mask 整组设置（位置控制 = 2552），已用本机 mavros-msgs 1.20.1 权威定义核实。
- **前置**:阶段1 假无人机链路全通（S0~S7 ✅）+ PX4 SITL 本机环境就绪（mavros 已装 ✅；PX4 v1.13.3 源码/EGM96/pymavlink 待装 ⬜）。
- **启动时机**:阶段三联调尾声 / 阶段四外场前。

### 3.3 任务跟踪

- 此两 spec 用本文件 §3 登记;启动时各自 `git commit` 创建 spec 骨架并从届时 spec 体系分支(避免与先导编码冲突)。
- 启动前先用 context7(`mcp__plugin_context7_context7__query-docs`)核实 PX4/mavros 最新文档并回填 §3.2 表(对齐 B spec §12 模式)。

---

> 本文件为补全 spec,不改变既有 spec 接口。G2/G4 启动时各自新建独立 spec。