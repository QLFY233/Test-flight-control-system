根据甲方要求，现在要对系统架构进行修改，请你先理解以下修改的内容，与用户核对，然后做出需要修改哪些spec的规划和合理性，然后进行修改，修改完之后，通读所有spec，检查错误，模糊，冲突以及不合理之处。 一切的起因是，要求中有一项“端侧小模型”的要求，为了实现这个要求，我们决定在无人机的机载电脑上运行一个数M大小的神经网络，输入是离散数据，每个输入端口分别涵盖：离散的环境数据（风速气温等），无人机的状态参数，动作编码和数值（例如，1号动作（上升），数值80（80m）），输出是对接ego-planner的输入，ego-planner所需输入的子集，包括目标点等。这个小模型设计的意义是针对无人机和环境状态做出下一步试飞动作的修正，取代原先的求解器结构。 因此，我们需要去掉求解器，求解器的工作交给ego-planner+小模型 那么由于小模型需要的是离散输入，我们需要修改α模型的输出，α模型应该输出json格式的动作编码和数值，然后由系统翻译成离散数据，输入给小模型 去掉了求解器之后，场地模型怎么避障呢？我们决定将设计改为，不预先编码场景，而是无人机在飞行过程中，根据雷达数据，ego-planner会自动避障。 在接下来的开发中，我们需要明确各个模块在通信总线上的位置，确保给出通用的接口（完成开放式接口规范） 还有，前端要提供数据展示看板，具体用哪些形式展示什么格式的数据是确定好的，然后由β模型用工具调用，替换实际展示哪些数据（也许提供筛选器功能？）。
