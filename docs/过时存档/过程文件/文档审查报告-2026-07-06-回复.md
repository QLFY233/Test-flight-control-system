以下回答可能有错误，如果你有不明白的地方可以指出
C1:统一为YYYYMMDDHHMMSS
C2:修改前端设计，β在各个页面占据相同的位置，切换页面或者tab时这部分不变，不要做成在不同页面不同位置，删去对α对话的部分
C3:收敛为单一路径，总体架构权威路径:β 调 propose_to_alpha(intent) → pending_proposal → /api/proposals/{id}/approve → 注入 α。
C4:以 B 为准,回写总体架构 §5.3 全表,并显式声明"先导用采样点法
C5:按照当前目录格式修复claude.md
C6:docs/设计目标.md
A1:统一为StatusMessage.flightStatus的6值
A2:A spec §4.2 补一节 SSE 事件枚举(text/tool_call_start/tool_call_result/plan),与前端 §4.2 逐字对齐。
A3:在总体架构 §2.2 约束/调度表后补 mode 取值与触发条件映射。
A4:明确"先导期 video_frame 暂不实现,VideoPanel 占位无信号;阶段2 PX4 + 相机接入再补",并修正冻结 §6 把 video_frame 与 voice_* 区分注释。
A5:把默认 field.yaml 的 capsule 改成 sphere
A6.在 Scene3D 入口处统一约定"所有 store.drone.position 用场地系 (x,y,z),Scene3D 内部转 Three.js (x,z,y)",并写到 §7.1 Scene3D 接口。
A7.A spec §5.5 末补一句(小模型 spec 已给出建议文案)。
A8.实现telemetry
A9.写明 /api/chat/beta 是一次 POST,Response 为 text/event-stream(server-sent events 流式);不存在"先建立 SSE 长连接再发消息"路径。
A10.A spec §4.1 给 POST /api/sessions/{id}/abort 的内部处理(发 abort 或 hover 到 solver、是否清 α 队列、是否同步 set status=aborted)。
A11.作为 get_current_environment() 工具
A12.增加移动端历史入口 Chat Tab 顶部"历史"按钮 → 半屏 HistoryPage
A13.明确 alpha_output.trajectory 是完整 TrajectorySpec
A14.区分"键盘全局快捷键"与"悬浮球点击/扇叶点击"两条触发路径
A15:前端 spec 补一节 PCM 采集(AudioContext + AudioWorklet,16k/16bit/mono,40ms 帧)
A16.明确语音末帧后由后端自动注入 β(无需前端再 POST),前端仅订阅已有 SSE;并区分"回填输入框等人按发送"与"自动发送"两种模式开关,开关放在设置里
A17.把状态标为"待装"
A18.直接冻结 to 在 pong 时取值
A19.现在进行context7查询
G1.补 spec(测试策略 + status 约定)
G2.加入todo
G3.改为"已设计,待联调核实"。
G4.加入todo
G5.按照你认为合理的方式做
G6.拷贝快照到 data/alpha_dataset/
G7.last pose 时间,llm 状态用llm的api
