# 后端 A (Python 3.10+) — 核心依赖
# 安装: python3.10 -m venv .venv-A && source .venv-A/bin/activate && pip install -r venv-A-requirements.txt

# Web 框架
fastapi==0.136.3
uvicorn==0.40.0
starlette==1.3.1
sse-starlette==3.4.6

# Pydantic AI (大模型调用)
pydantic-ai==2.0.0
pydantic-ai-slim==2.0.0
pydantic==2.13.4
pydantic-core==2.46.4
pydantic-settings==2.14.2

# OpenAI 兼容客户端 (DeepSeek 等)
openai==2.48.0
httpx==0.28.1
httpcore==1.0.9

# 数据库
SQLAlchemy==2.0.51
aiosqlite==0.22.1
greenlet==3.5.4

# WebSocket
websockets==16.1.1

# 消息序列化
msgpack==1.2.1

# 配置
PyYAML==6.0.3
python-dotenv==1.2.2

# 语音 (讯飞 STT/TTS)
cryptography==49.0.0
